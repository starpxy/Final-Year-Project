{"author": "Theano", "code": "from __future__ import absolute_import, print_function, division\nimport pycuda.autoinit\nimport pycuda.driver as drv\nimport numpy as np\n\nfrom pycuda.compiler import SourceModule\nmod = SourceModule(\"\"\"\n__global__ void multiply_them(float *dest, float *a, float *b)\n{\n  const int i = threadIdx.x;\n  dest[i] = a[i] * b[i];\n}\n\"\"\")\n\nmultiply_them = mod.get_function(\"multiply_them\")\n\na = np.random.randn(400).astype(np.float32)\nb = np.random.randn(400).astype(np.float32)\n\ndest = np.zeros_like(a)\nmultiply_them(\n        drv.Out(dest), drv.In(a), drv.In(b),\n        block=(400,1,1), grid=(1,1))\n\nassert np.allclose(dest, a*b)\nprint(dest)\n", "comments": "      global   void multiply them(float  dest  float   float  b)     const int   threadidx x    dest       b         ", "content": "from __future__ import absolute_import, print_function, division\nimport pycuda.autoinit\nimport pycuda.driver as drv\nimport numpy as np\n\nfrom pycuda.compiler import SourceModule\nmod = SourceModule(\"\"\"\n__global__ void multiply_them(float *dest, float *a, float *b)\n{\n  const int i = threadIdx.x;\n  dest[i] = a[i] * b[i];\n}\n\"\"\")\n\nmultiply_them = mod.get_function(\"multiply_them\")\n\na = np.random.randn(400).astype(np.float32)\nb = np.random.randn(400).astype(np.float32)\n\ndest = np.zeros_like(a)\nmultiply_them(\n        drv.Out(dest), drv.In(a), drv.In(b),\n        block=(400,1,1), grid=(1,1))\n\nassert np.allclose(dest, a*b)\nprint(dest)\n", "description": "Theano is a Python library that allows you to define, optimize, and evaluate mathematical expressions involving multi-dimensional arrays efficiently. It can use GPUs and perform efficient symbolic differentiation.", "file_name": "pycuda_simple.py", "id": "9c3e0ed54ab29d074c8274029f7dd832", "language": "Python", "project_name": "Theano", "quality": "", "save_path": "/home/ubuntu/test_files/clean/python/Theano-Theano/Theano-Theano-546067d/doc/hpcs2011_tutorial/pycuda_simple.py", "save_time": "", "source": "", "update_at": "2018-03-18T03:16:17Z", "url": "https://github.com/Theano/Theano", "wiki": true}